clean:
	npm run clean

test:
	npm run test

start:
	npm run start

serve:
	npm run serve

serve-dist:
	npm run serve-dist

tsd:
	npm run tsd

tsd-update:
	npm run tsd-update

update:
	npm run update

outdated:
	npm run outdated

install:
	npm run setup

setup:
	npm run setup
